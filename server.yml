# server.yml de tu contenedor verapdf/rest
verapdf:
  recordPasses:      true   # incluirá también los checks que pasan
  showErrorMessages: true   # detallará el mensaje de cada check
  # opcionales:
  # maxFails: -1           # reportar todos los failures
  # isLogsEnabled: true    # si quieres también logs
server:
  applicationConnectors:
    - type: http
      port: ${PORT:-8080}
